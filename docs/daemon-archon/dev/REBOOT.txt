# daemon-archon 开发继续任务提示词

## 项目概述

**daemon-archon** 是一个 Claude Code 插件，用于实现定时任务调度和长期任务监控。

### 核心功能
- **Probe 模式**：监控长期运行的 Claude Code 会话，定期检查任务状态
- **Cron 模式**：定期启动临时 Claude Code 会话执行预定义的 workflow 任务
- **跨平台调度**：支持 Linux (Crontab/Systemd)、Windows (Task Scheduler)、macOS (Launchd)
- **通知系统**：可选的通知扩展（系统通知、Slack、Webhook、飞书）

### 技术架构
- **后端**：FastAPI + APScheduler（统一调度接口）
- **前端**：Claude Code 命令行插件
- **存储**：本地文件系统（`~/.claude/daemon-archon/`）
- **语言**：Python 3.8+

---

## 当前开发状态

### 已完成功能（v1.0.0）
✅ 基础架构设计完成
✅ 跨平台调度器设计（SchedulerManager）
✅ Probe 模式设计（archon_check.py）
✅ 通知系统设计（notifier.py，支持 system/slack/webhook）
✅ 设计文档完善（design.md, dev_design.md）

### 最近重要变更
🔄 **飞书通知调整为可选扩展**（Decision #14）
- 核心功能**不依赖**通知系统
- 飞书作为可选扩展，通过 `/add-feishu-channel` 命令安装配置
- 通知失败不影响任务执行（降级策略）

---

## 待实现功能（按优先级）

### 🔴 高优先级（核心功能）

1. **Cron 执行引擎完善**（REBOOT.txt 原内容指出的遗漏点）
   - [ ] 实现 `cron_executor.py` - Cron 任务执行器
   - [ ] 定义 workflow 格式规范（workflow.md）
   - [ ] 实现 workflow → Claude Code CLI 提示词转换
   - [ ] 实现结果分析机制（判断"发现问题"的标准）
   - [ ] 实现执行超时处理（timeout_minutes 配置）
   - [ ] 解决环境变量问题（PATH、工作目录）

2. **卡住检测机制**（参考 OpenClaw 设计）
   - [ ] 实现 `task_stuck_detector.py`（已有设计文档 cron_by_openclaw.md）
   - [ ] Probe 卡住检测（1小时无 transcript 更新）
   - [ ] Archon 检查卡住检测（5分钟超时）
   - [ ] Cron 任务卡住检测（基于 timeout_minutes）
   - [ ] 独立运行的检测器（不依赖正常检查周期）

3. **状态存储与追踪**
   - [ ] 实现 `state_store.py` - 任务状态持久化
   - [ ] 记录运行次数、错误统计、执行时长
   - [ ] 实现状态查询接口（`/status` 命令）

### 🟡 中优先级（增强功能）

4. **飞书通知扩展**（可选功能）
   - [ ] 实现 `/add-feishu-channel` 命令
   - [ ] 引导式配置流程（App ID、App Secret、domain）
   - [ ] 依赖安装检查（feishu-python-sdk）
   - [ ] 单向通知实现（系统 → 飞书）
   - [ ] 交互式卡片支持（按钮、Markdown）
   - [ ] 双向通信（飞书 → 系统，Phase 2）

5. **命令行工具完善**
   - [ ] `/probe` - 启动 Probe 模式监控
   - [ ] `/cron` - 配置 Cron 定时任务
   - [ ] `/status` - 查看所有任务状态
   - [ ] `/check-stuck` - 手动触发卡住检测
   - [ ] `/logs` - 查看任务日志

### 🟢 低优先级（优化功能）

6. **高级特性**
   - [ ] WebSocket 长连接（飞书实时消息）
   - [ ] 群聊支持（groupPolicy、groupAllowFrom）
   - [ ] 多任务并发执行
   - [ ] 任务依赖关系管理
   - [ ] Web UI 控制面板

---

## 关键设计文档

### 主要文档位置
- **设计文档**：`docs/daemon-archon/dev/design.md`（用户需求、架构设计）
- **开发设计**：`docs/daemon-archon/dev/dev_design.md`（技术决策、实现细节）
- **参考文档**：
  - `docs/daemon-archon/dev/cron_by_openclaw.md`（卡住检测设计）
  - `docs/daemon-archon/dev/reference/openclaw/定时任务.md`（OpenClaw 参考）
  - `reference/feishu-openclaw-cn/`（飞书插件参考实现）

### 关键设计决策

**Decision #14: 飞书通知机制（可选扩展）**
- **核心原则**：daemon-archon 核心功能**不依赖**通知系统
- **实现方式**：通过 `/add-feishu-channel` 命令安装配置
- **降级策略**：通知失败返回 True，不影响任务执行
- **配置位置**：`~/.claude/daemon-archon/config.json` 中的 `notification` 字段

**通知方法优先级**：
1. `none`（默认）- 无通知
2. `system` - 系统通知
3. `slack` - Slack Webhook
4. `webhook` - 自定义 Webhook
5. `feishu` - 飞书企业应用（可选扩展）

---

## 代码结构

### 工作目录
```
/data/zyw/nero-cc-marketplace/
├── plugins/daemon-archon/           # 插件主目录
│   ├── .claude-plugin/
│   │   └── plugin.json              # 插件元数据
│   ├── commands/                    # 命令定义（.md 文件）
│   ├── scripts/
│   │   ├── server/
│   │   │   ├── archon_main.py       # 后台主进程
│   │   │   ├── scheduler_manager.py # 调度器管理
│   │   │   ├── notifier.py          # 通知系统（已实现）
│   │   │   ├── cron_executor.py     # ❌ 待实现
│   │   │   ├── state_store.py       # ❌ 待实现
│   │   │   └── task_stuck_detector.py # ❌ 待实现
│   │   ├── probe/
│   │   │   └── archon_check.py      # Probe 检查逻辑
│   │   └── utils/
│   │       └── analyzer.py          # 结果分析器
│   └── README.md
├── docs/daemon-archon/dev/          # 开发文档
│   ├── design.md                    # 设计文档（7章）
│   ├── dev_design.md                # 开发设计（14个决策）
│   ├── cron_by_openclaw.md          # 卡住检测设计
│   └── REBOOT.txt                   # 本文件
└── reference/feishu-openclaw-cn/    # 飞书参考实现
```

### 关键文件说明

**notifier.py**（已实现）
- 支持 system/slack/webhook 通知
- `send_notification(title, message, level)` 方法
- 降级策略：失败返回 True

**archon_check.py**（已设计）
- Probe 模式的检查逻辑
- 读取 transcript.jsonl 分析任务状态
- 调用 analyzer.py 判断是否需要通知

**cron_executor.py**（待实现）
- 读取 workflow.md 定义
- 启动临时 Claude Code CLI 会话
- 执行任务并分析结果
- 记录执行状态到 state_store

**task_stuck_detector.py**（已有设计）
- 参考 `cron_by_openclaw.md` 实现
- 独立运行，不依赖正常检查周期
- 检测三种卡住场景（Probe/Archon/Cron）

---

## 开发建议

### 下一步行动

**建议优先实现顺序**：
1. **Cron 执行引擎**（核心功能，用户最关心的遗漏点）
   - 先定义 workflow.md 格式规范
   - 实现 cron_executor.py 基础版本
   - 测试临时会话启动和结果获取

2. **卡住检测机制**（提升可靠性）
   - 实现 task_stuck_detector.py
   - 集成到 archon_check.py 作为前置检查
   - 添加 `/check-stuck` 命令

3. **状态存储**（支撑其他功能）
   - 实现 state_store.py
   - 记录任务执行历史
   - 提供状态查询接口

4. **飞书扩展**（可选功能，最后实现）
   - 实现 `/add-feishu-channel` 命令
   - 单向通知（系统 → 飞书）
   - 交互式卡片

### 技术要点

**Workflow 格式建议**：
```markdown
# workflow.md

## Task: 检查代码质量

### Prompt
请检查项目中的 Python 代码质量，重点关注：
1. 是否有未使用的导入
2. 是否有过长的函数（>50行）
3. 是否有重复代码

### Expected Output
- 问题列表（如果有）
- 建议修复方案

### Trigger Notification If
- 发现任何问题
```

**环境变量处理**（Cron 执行）：
```python
# cron_executor.py 中设置环境
import os
env = os.environ.copy()
env['PATH'] = f"/usr/local/bin:/usr/bin:{env.get('PATH', '')}"
env['CLAUDE_PROJECT_PATH'] = project_path

subprocess.run(['claude', 'code', ...], env=env, cwd=project_path)
```

**超时处理**：
```python
import subprocess
try:
    result = subprocess.run(
        ['claude', 'code', ...],
        timeout=timeout_minutes * 60,
        capture_output=True
    )
except subprocess.TimeoutExpired:
    # 记录超时，触发通知
    pass
```

---

## 测试建议

### 单元测试
- `test_notifier.py` - 测试通知降级策略
- `test_cron_executor.py` - 测试 workflow 解析和执行
- `test_stuck_detector.py` - 测试卡住检测逻辑

### 集成测试
1. 创建测试任务（Probe + Cron）
2. 模拟卡住场景（长时间无输出）
3. 验证通知发送（各种方法）
4. 验证状态记录（state_store）

### 手动测试流程
```bash
# 1. 安装插件
/plugin install daemon-archon@nero-cc-marketplace

# 2. 启动 Probe 监控
/probe --session-id <session-id> --check-interval 5

# 3. 配置 Cron 任务
/cron --workflow ./workflow.md --schedule "*/10 * * * *"

# 4. 查看状态
/status

# 5. 检查卡住
/check-stuck

# 6. 配置飞书（可选）
/add-feishu-channel
```

---

## 常见问题

### Q1: Cron 任务如何判断"发现问题"？
**A**: 在 workflow.md 中定义 `Trigger Notification If` 条件，cron_executor.py 解析 Claude 输出，匹配关键词或模式。

### Q2: 如何确保 Cron 环境能找到 claude 命令？
**A**: 在 cron_executor.py 中显式设置 PATH 环境变量，包含 `/usr/local/bin`、`/usr/bin` 等常见路径。

### Q3: 飞书通知失败会影响任务执行吗？
**A**: 不会。notifier.py 实现了降级策略，失败时返回 True，任务继续执行。

### Q4: 如何支持不同任务的不同执行间隔？
**A**: 建议方案 A（每个任务一个 cron 条目），在 scheduler_manager.py 中为每个任务注册独立的调度器。

### Q5: 卡住检测会影响性能吗？
**A**: 不会。task_stuck_detector.py 独立运行，只检查文件修改时间和配置，不解析大文件。

---

## 参考资源

### 外部参考
- **OpenClaw Cron 系统**：`docs/daemon-archon/dev/reference/openclaw/定时任务.md`
- **OpenClaw 飞书插件**：`reference/feishu-openclaw-cn/src/`
- **飞书开放平台文档**：https://open.feishu.cn/document/

### 内部文档
- **设计文档**：`docs/daemon-archon/dev/design.md`（第7章：通知系统设计）
- **开发设计**：`docs/daemon-archon/dev/dev_design.md`（Decision #14：飞书通知）
- **卡住检测设计**：`docs/daemon-archon/dev/cron_by_openclaw.md`

---

## 开发环境

### 依赖安装
```bash
pip install fastapi uvicorn apscheduler psutil anthropic
# 飞书扩展（可选）
pip install feishu-python-sdk
```

### 环境变量
```bash
# Claude API（用于 AI 分析）
export ANTHROPIC_API_KEY="sk-ant-..."

# 飞书配置（可选）
export FEISHU_APP_ID="cli_..."
export FEISHU_APP_SECRET="..."
```

### 工作目录
- **插件代码**：`/data/zyw/nero-cc-marketplace/plugins/daemon-archon/`
- **文档**：`/data/zyw/nero-cc-marketplace/docs/daemon-archon/dev/`
- **运行时数据**：`~/.claude/daemon-archon/`

---

## 总结

**当前状态**：设计阶段完成，核心功能待实现

**下一步**：优先实现 Cron 执行引擎和卡住检测机制

**关键原则**：
1. 核心功能独立于通知系统
2. 通知失败不影响任务执行
3. 飞书作为可选扩展
4. 参考 OpenClaw 的优秀设计

**开发建议**：先实现核心功能（Cron + 卡住检测），再实现增强功能（飞书扩展）

---

**最后更新**：2026-02-06
**版本**：v1.0.0（设计完成，待实现）
